import torch.nn as                              nn

class decoder(nn.Module):

    # ------------------------------------------------------------------------------------------------------------------
    def __init__(self):
        a=1
        # self.conv1 = nn.Conv2d(in_size, out_size, kernel_size=4, stride=2, padding=1)
        # self.conv2 = nn.Conv2d(in_size, out_size, kernel_size=4, stride=2, padding=1)
        # self.conv3 = nn.Conv2d(in_size, out_size, kernel_size=4, stride=2, padding=1)
        #
        # self.upconv1 = nn.ConvTranspose2d(in_size, out_size, kernel_size=4, stride=2, padding=1)
        # self.upconv2 = nn.ConvTranspose2d(in_size, out_size, kernel_size=4, stride=2, padding=1)
        # self.upconv3 = nn.ConvTranspose2d(in_size, out_size, kernel_size=4, stride=2, padding=1)

    # ------------------------------------------------------------------------------------------------------------------
    def forward(self, skc1, skc2):

        a=1

