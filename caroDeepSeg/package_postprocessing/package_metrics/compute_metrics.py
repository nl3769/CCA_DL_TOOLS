import numpy                            as np

# ----------------------------------------------------------------------------------------------------------------------------------------------------
def compute_MAE(arr1, arr2):

    err = np.abs(arr1-arr2)
    err_mean = np.mean(err)
    err_std  = np.std(err)

    return err_mean, err_std


# ----------------------------------------------------------------------------------------------------------------------------------------------------
